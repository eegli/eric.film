import styled from 'styled-components';

// Wrapper that can be used on pages for dynamic imports.
export const SpinnerWrapper = styled.div`
  position: absolute;
  display: flex;
  justify-content: center;
  align-items: center;
  top: 3rem;
  width: 100%;
`;
